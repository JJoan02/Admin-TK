# Variables de entorno para Admin-TK
# Copia este archivo a .env y rellena los valores para el desarrollo local.
# En producción (Pterodactyl), estas variables deben configurarse en el panel de entorno.

# Clave de API para el servicio de IA de Google Gemini (OBLIGATORIA)
GEMINI_API_KEY=

# Puerto para el servidor web (opcional, por defecto 3000)
PORT=3000

# Nivel de registro (opcional, por defecto 'info'. Otros: 'debug', 'warn', 'error')
LOG_LEVEL=info

# ID de la sesión del bot (opcional, por defecto 'admin_tk_session')
SESSION_ID=admin_tk_session
